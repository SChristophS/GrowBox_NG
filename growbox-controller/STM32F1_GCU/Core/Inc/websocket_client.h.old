#ifndef WEBSOCKET_CLIENT_H
#define WEBSOCKET_CLIENT_H

#include <stdint.h>
#include <stddef.h> // Für size_t

#define SOCK_TCP 2

typedef struct {
    int8_t sock;
    const char *server_ip;
    uint16_t server_port;
    uint16_t local_port;
} SocketNetwork;

//int socket_network_init(SocketNetwork *s, uint16_t port, const char *path);
void socket_network_init(SocketNetwork *n, const char *server_ip, uint16_t server_port, uint16_t local_port);
int websocket_send(int socket, const char *message);
int websocket_recv(int socket, char *message, size_t max_len);
int websocket_handshake(int socket, const char *host, const char *path);



#endif // WEBSOCKET_CLIENT_H
